# 记账 App — 产品文档

> 本文档是《记账》桌面应用的唯一权威产品文档。所有开发工作以本文档为准。
> 本文档由 Claude 与用户共同确认（2026-08-15）。

## 1. 项目概览

- **项目名称**：记账
- **产品定位**：个人桌面记账应用，帮助用户记录每一笔人民币收支（支出 + 收入）
- **目标平台**：Windows 10/11、macOS
- **界面语言**：简体中文
- **货币单位**：人民币（¥）

## 2. 技术栈（用户已决策，不得擅自更换）

- **桌面框架**：Electron —— 用户从 Tauri / Electron / Flutter / 网页应用 4 个方案中选定（2026-08-15）
- **界面框架**：React + TypeScript —— 用户从 React / Vue 中选定（2026-08-15）
- **UI 组件库**：Ant Design（中文生态成熟）
- **数据存储**：SQLite（本地数据库文件）—— 用户选定（2026-08-15）
- **图表库**：ECharts（中文文档完善，饼图/柱状图成熟）
- **打包发布**：electron-builder —— Windows 生成 .exe 安装包；macOS 生成 .dmg（需在 Mac 电脑上构建，或用 GitHub Actions 云构建）
- 后续新增技术依赖时，须按第 7 节协作规则执行

## 3. 功能需求（标准版）

### 3.1 记账（核心功能）

记一笔花销，包含以下字段：

| 字段 | 必填 | 说明 |
|---|---|---|
| 类型 | ✅ | 支出 / 收入（切换后分类自动换成对应体系） |
| 金额 | ✅ | 数字，支持小数点后 2 位 |
| 分类 | ✅ | 先选一级大类，再选二级小类（见第 4 节） |
| 日期 | ✅ | 默认今天，可修改 |
| 备注 | ❌ | 可选，最多 100 字 |

- 首页常驻「记一笔」按钮，支持键盘快捷键 Ctrl+N（macOS 为 Cmd+N）
- 保存后可快速连续记录下一笔（连续记账时类型保持不变）

### 3.2 流水管理

- 流水列表按日期倒序排列，按天分组显示（今天 / 昨天 / 具体日期）
- 每笔显示：分类图标、二级小类名、备注、金额（支出红色，收入绿色带 + 号）
- 每天小计分「支出 / 收入」两段显示（为 0 的段不显示）
- 支持按备注关键词搜索、按分类筛选
- 支持编辑、删除（删除需二次确认）

### 3.3 首页（本月概览）

- 本月总支出、本月总收入、本月结余（收入 − 支出，负数表示超支）
- 今日支出与今日收入（同一卡片两行显示）
- 本月笔数与日均支出（附在「最近流水」标题右侧）
- 最近 5 笔流水
- 月份可切换查看（左右箭头），默认本月

### 3.4 统计

- 支持「支出 / 收入」切换，图表随切换分析对应类型（月份切换两者共用）
- 分类占比饼图（默认本月，可切换月份）
- 每日收支柱状图（当月每一天；支出红、收入绿）
- 一级分类金额排行列表

### 3.5 分类管理

- 内置分类体系见第 4 节（支出 10 个 + 收入 5 个一级大类，均固定不可修改）
- 用户可添加 / 删除自定义二级小类（支出、收入分类均支持）
- 已被使用的分类不可删除（保护历史数据）

## 4. 分类体系（支出与收入各一套，均为两级：一级大类 → 二级小类）

### 支出分类

| 一级大类 | 二级小类 |
|---|---|
| 🍜 餐饮 | 早餐、午餐、晚餐、零食、饮料咖啡、外卖、聚餐 |
| 🚗 交通 | 公交地铁、打车、火车高铁、飞机、加油、停车费 |
| 🏠 居住 | 房租、水电燃气、物业费、宽带网络、家居用品、维修维护 |
| 🛒 购物 | 日用品、服装鞋包、数码电子、美妆护肤、家居家电 |
| 🎮 娱乐 | 电影演出、游戏、旅游出行、运动健身、会员订阅 |
| 💊 医疗健康 | 看病买药、体检保健 |
| 📚 学习教育 | 书籍教材、课程培训、文具用品 |
| 🧧 人情往来 | 红包礼金、礼物赠送、请客 |
| 📱 通讯 | 手机话费、网络流量 |
| 📦 其他 | 其他支出 |

### 收入分类（2026-08-15 新增）

| 一级大类 | 二级小类 |
|---|---|
| 💰 工资薪酬 | 月薪工资、奖金提成、加班补贴 |
| 📈 投资理财 | 利息收益、股票基金、房租收入 |
| 🧧 红包礼金 | 红包收入、礼金收入 |
| 💼 兼职外快 | 兼职收入、稿费、副业收入 |
| 📦 其他收入 | 其他收入 |

## 5. 数据与隐私

- 所有账本数据保存在用户本机，不上传任何网络
- 数据文件位置：
  - Windows：`%APPDATA%\jizhang\`
  - macOS：`~/Library/Application Support/jizhang/`

## 6. 界面设计要点

- 左侧导航：首页 / 流水 / 统计 / 设置
- 浅色简洁主题，中文界面
- 金额统一显示为 ¥ + 两位小数
- 支出金额使用红色系显示，收入金额使用绿色系并带 + 号（符合国内记账习惯）

## 7. 与 Claude 的协作规则（重要！贯穿整个项目期）

**用户背景**：不懂编程、首次使用 Claude Code。以下规则必须严格遵守：

1. **技术决策必须由用户拍板**：任何技术选型（框架、库、工具），Claude 必须先列出 2-4 个方案，用通俗语言解释每个方案「是什么、优点、缺点」，由用户选择决定。Claude 不得擅自替用户决定。
2. 解释技术问题时用通俗语言，避免堆砌专业术语；必须使用术语时先解释其含义。
3. 用户的需求描述优先；需求模糊时主动向用户提问确认，不擅自假设。
4. 所有与用户的沟通、代码注释、文档一律使用中文。
5. 每次完成一项开发后，用通俗语言向用户说明「做了什么、如何测试、遇到什么问题」。
6. 涉及删除数据等不可逆操作，必须先向用户确认。

## 8. 开发里程碑

- [x] M1：产品文档（本文档）
- [x] M2：项目搭建 + 记账功能（记一笔、分类选择）
- [x] M3：流水管理（按天分组、搜索、筛选、编辑、删除）+ 首页本月概览
- [x] M4：统计图表（分类占比饼图、每日支出柱状图、分类排行）+ 分类管理（自定义小类增删）
- [x] M5：打包发布 —— Windows 安装包已完成（dist/记账 Setup 0.1.0.exe）；**Mac 版用户决定暂缓**（2026-08-15，方案：有 Mac 后本机打包或 GitHub Actions 云构建）
- [x] M6：收入功能 —— 收入分类体系、结余统计、统计页收支切换（2026-08-15）
- [x] M7：贪吃蛇小游戏 —— 左侧栏游戏入口、Canvas 手写、经典模式（2026-08-15，在 snake-game 分支上开发，用户试玩确认后已合并回主线；**2026-08-24 应用户要求移除，相关代码与文档已删除**）

## 9. 已知说明

- 当前开发机为 Windows 10：Windows 版安装包可在本机直接构建；macOS 版安装包需在 Mac 电脑上构建（或使用 GitHub Actions 免费云构建），代码本身跨平台无需改动。
- **开发环境坑（重要）**：在 VSCode 环境中 `ELECTRON_RUN_AS_NODE=1` 环境变量会被设置，导致 Electron 以纯命令行模式启动而报错 `Cannot read properties of undefined (reading 'isPackaged')`。启动应用前必须先清除该变量：`unset ELECTRON_RUN_AS_NODE && npm run dev`（Windows 的 cmd 用 `set ELECTRON_RUN_AS_NODE=`）。已用此法验证可正常弹出应用窗口（2026-08-15）。
- **Windows 打包坑（重要）**：electron-builder 解压 winCodeSign 工具时会在 darwin 符号链接上失败（Windows 无管理员权限无法创建符号链接，报 `Cannot create symbolic link`）。已解决：手动把解压产物复制到 `%LOCALAPPDATA%\electron-builder\Cache\winCodeSign\winCodeSign-2.6.0`（缺的 2 个 darwin 文件不影响 Windows 打包）。以后重新打包若换新版本 winCodeSign 需重做此步骤。构建命令建议带国内镜像：`ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/ CSC_IDENTITY_AUTO_DISCOVERY=false npm run build:win`。
- **数据目录已固定**：main 进程中 `app.setPath('userData', join(app.getPath('appData'), 'jizhang'))`，保证开发版与安装版共用同一账本 `%APPDATA%\jizhang\jizhang.db`（与第 5 节约定一致）。
- **数据库自动升级**（2026-08-15 起）：每次启动会检查表结构，老账本缺「收支类型」列时自动补列（只加列不删数据），迁移前自动把数据库备份为同目录 `jizhang.db.bak`；内置收入分类首次启动新版时自动补种。
- 应用图标由 `scripts/make-icon.js` 纯 Node 生成（build/icon.png，512x512 品牌红 ¥），修改图标后重跑该脚本即可（本机 PowerShell 会卡死，勿用）。

## 10. Git 存档约定（2026-08-15 用户确认）

- 项目已纳入 git 管理：本地仓库 + **GitHub 云端备份**（私有仓库 https://github.com/tanjin0310/jizhang-app，只有用户自己可见；2026-08-15 接入，登录方式为 SSH 电子钥匙，配一次永久有效）。
- 日常工作流：**每完成一项功能改动，自动创建存档（commit）**，存档备注用中文简述本次改动；用户说「存档」时立即存档。**每次存档后顺手同步到云端**（git push）。
- 用户不懂 git：任何 git 操作执行前，先用通俗语言讲解要做什么、有何影响，征得用户同意后再执行；不执行会改变历史的操作（如 rebase、reset 硬回退）除非用户明确要求。
- 排除清单（.gitignore）：node_modules、out、dist、日志不进存档；`.claude/settings.local.json`（Claude Code 本机权限设置，每次运行命令自动变化，与软件代码无关）不进存档；账本数据在项目文件夹之外，与 git 无关，隐私安全。
- 仓库本机身份：用户名 jizhang、邮箱 jizhang@local（仅本地标识，不对外）。
